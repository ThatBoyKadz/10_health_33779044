<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Logged In - Health Tracker</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f5f7fa;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
        }
        .container {
            background: white;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0,0,0,0.1);
            max-width: 500px;
            text-align: center;
        }
        h2 {
            color: #4CAF50;
            margin-bottom: 1rem;
        }
        a.button {
            display: block;
            margin: 0.5rem auto;
            padding: 0.7rem 1.5rem;
            background: #4CAF50;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            width: fit-content;
        }
        a.button.logout {
            background: #f44336;
        }
    </style>
</head>
<body>

<div class="container">
    <h2>Welcome, <%= first %> <%= last %>!</h2>
    <p>You are logged in as <strong><%= username %></strong>.</p>

    <a class="button" href="/health/add">Add New Health Record</a>
    <a class="button" href="/health">View Health Records</a>
    <a class="button logout" href="/users/logout">Logout</a>
    <a class="button" href="/">return home</a>
</div>

</body>
</html>
